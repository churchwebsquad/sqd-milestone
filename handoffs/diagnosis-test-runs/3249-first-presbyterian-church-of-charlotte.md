# Diagnosis test run — First Presbyterian Church of Charlotte (3249)

Generated: 2026-06-27T01:15:25.947Z
Total discovery sections: 76
Schemas: feature_card=11, (unclassified)=11, person_card=52, career_card=1, blog_post_card=1

## /worship · 5 sections

**Adult Ensembles**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Youth Ensembles**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Children's Ensembles**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Signature Events Every Year**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Meet the Music Ministry Team**
- 5 items
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":5}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /care · 3 sections

**Your Pastoral Team**
- 3 items
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":3}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

**Content Section 99**
- 2 items
- schema: `feature_card` (low)
- template: `` target: unknown
- cta: {"no_link":2}
- in template: name=2/2  description=2/2
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

**Content Section 99**
- 2 items
- schema: `feature_card` (low)
- template: `` target: unknown
- cta: {"no_link":2}
- in template: name=2/2  description=2/2
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /children-youth · 3 sections

**Children's Choir**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Youth Music**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**Meet Your Leader**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":1}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /local-global · 3 sections

**Serving Charlotte**
- 6 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"no_link":6}
- in template: name=6/6  description=6/6
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

**Serving the World**
- 4 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"no_link":4}
- in template: name=4/4  description=4/4
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

**Outreach Leadership**
- 3 items
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":3}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /about · 2 sections

**Rooted in Reformed Faith, Open to the Whole Story**
- 2 items
- schema: `feature_card` (low)
- template: `` target: unknown
- cta: {"no_link":2}
- in template: name=0/2  description=2/2
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

**Key beliefs held at First Presbyterian Church**
- 3 items
- schema: `feature_card` (low)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: name=3/3  description=3/3
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /advocacy · 2 sections

**Areas of Focus**
- 5 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"no_link":5}
- in template: name=5/5  description=5/5
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

**Learn More**
- 1 item
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":1}
- in template: show_search=1/1  show_category=1/1  category_label=1/1  show_tag=1/1  tag_label=1/1  show_sort=1/1  sort_label=1/1

## /new · 2 sections

**Kids & Families**
- 3 items
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":3}
- in template: image=0/3

**When You're Ready to Call This Home**
- 2 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"no_link":2}
- in template: name=2/2  description=2/2
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /staff · 2 sections

**Pastoral Staff**
- 6 items
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":6}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

**Program & Support Staff**
- 39 items
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"no_link":39}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /home · 1 section

**Faith That Lives Beyond Sunday**
- 3 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"internal_route":2,"no_link":1}
- in template: name=3/3  description=3/3
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /employment · 1 section

**Open Positions**
- 1 item
- schema: `career_card` (high)
- template: `(CPT-bound) ` target: flat-list
- cta: {"file_download":1}
- in template: title=1/1  location=1/1

## /serve · 1 section

**Serve Your City**
- 5 items
- schema: `feature_card` (medium)
- template: `` target: unknown
- cta: {"no_link":5}
- in template: name=5/5  description=5/5
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /the-history-hallway · 1 section

**Triptych**
- 2 items
- schema: `feature_card` (low)
- template: `` target: unknown
- cta: {"no_link":2}
- in template: name=2/2  description=2/2
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)
- ⚠ upstream_compression_loss on schema `feature_card` — dropped: cta_label, cta_url (medium)

## /watch · 1 section

**Catch Up on Any Message**
- 1 item
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":1}
- in template: show_search=1/1  show_category=1/1  category_label=1/1  show_tag=1/1  tag_label=1/1  show_sort=1/1  sort_label=1/1

## /single-staff · 1 section

**Staff Member Name**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /single-event · 1 section

**Single Event Section 4**
- 1 item
- schema: (unclassified)
- template: `(CPT-bound) ` target: individual-page
- partner context: events, display=`wordpress`, external=https://firstpres-charlotte.org/events/grands-camp/

## /staff/lewis-galloway · 1 section

**Lewis Galloway**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/anna-dickson · 1 section

**Anna Dickson**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /news-media · 1 section

**Category Filter 4**
- 1 item
- schema: (unclassified)
- template: `` target: unknown
- cta: {"no_link":1}
- in template: show_category=1/1  show_tag=1/1  tag_label=1/1  show_sort=1/1  sort_label=1/1

## /staff/lucy-crain · 1 section

**Lucy Crain**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /single-blog-post · 1 section

**Blog Post Heading**
- 1 item
- schema: `blog_post_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: title=1/1
- ⚠ upstream_compression_loss on schema `blog_post_card` — dropped: date, body (high)
- ⚠ upstream_compression_loss on schema `blog_post_card` — dropped: author, date, body, category (high)
- ⚠ upstream_compression_loss on schema `blog_post_card` — dropped: author, date, excerpt, url (high)

## /staff/chuck-williamson · 1 section

**Chuck Williamson**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/mary-henderson-bowman · 1 section

**Mary Henderson Bowman**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/rob-spach · 1 section

**Rob Spach**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/andrea-nelson · 1 section

**Andrea Nelson**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/anita-bennett · 1 section

**Anita Bennett**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/audrey-webb-kahrs · 1 section

**Audrey Webb Kahrs**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/barb-frith · 1 section

**Barb Frith**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/ben-treece · 1 section

**Ben Treece**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/bradley-thompson · 1 section

**Bradley Thompson**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/brandon-thomas · 1 section

**Brandon Thomas**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/caroline-schaal · 1 section

**Caroline Schaal**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/cierra-christian · 1 section

**Cierra Christian**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/crystal-sellers · 1 section

**Crystal Sellers**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/diane-maye · 1 section

**Diane Maye**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/flo-bryan · 1 section

**Flo Bryan**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/garrell-keesler · 1 section

**Garrell Keesler**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/gloria-clark · 1 section

**Gloria Clark**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/heidi-squires · 1 section

**Heidi Squires**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/jan-gaddis · 1 section

**Jan Gaddis**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/jane-ives · 1 section

**Jane Ives**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/kaitlyn-davros · 1 section

**Kaitlyn Davros**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/kai · 1 section

**Kai**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/kalia-coffey · 1 section

**Kalia Coffey**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/kelly-deal · 1 section

**Kelly Deal**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/kelly-harder · 1 section

**Kelly Harder**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/latonya-hope · 1 section

**LaTonya Hope**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/laura-katute · 1 section

**Laura Katute**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/lauren-mccrory · 1 section

**Lauren McCrory**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/lauren-russell · 1 section

**Lauren Russell**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/lucy-baum · 1 section

**Lucy Baum**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/mark-pestano · 1 section

**Mark Pestano**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/michael-denise · 1 section

**Michael DeNise**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/michael-o-rourke · 1 section

**Michael O'Rourke**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/ray-vargas · 1 section

**Ray Vargas**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/salesia-dudley · 1 section

**Salesia Dudley**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/semra-staut · 1 section

**Semra Staut**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/sofia-di-dio · 1 section

**Sofia Di Dio**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/tammy-hatley · 1 section

**Tammy Hatley**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/tanya-gaspar · 1 section

**Tanya Gaspar**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/trevor-cook · 1 section

**Trevor Cook**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/will-celestine · 1 section

**Will Celestine**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

## /staff/will-young · 1 section

**Will Young**
- 1 item
- schema: `person_card` (high)
- template: `(CPT-bound) ` target: individual-page
- cta: {"no_link":1}
- in template: name=1/1
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, email, headshot, ministry_area (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, bio, headshot (high)
- ⚠ upstream_compression_loss on schema `person_card` — dropped: role, headshot (high)

---

## Inventory-layer discovery (35 concepts)

Pre-binding diagnostic — what schemas the partner HAS in source even if no bound section exists yet.

**Adult Ministry** (topic: `inv:adults:program`)
- 7 items (of 16 total in topic), dominant kind: `program`
- diagnosed: `feature_card` (low)
- fields with data: name=7/7  description=7/7  cta_label=4/7  cta_url=4/7

**Adult Ministry** (topic: `inv:adults:detail`)
- 4 items (of 16 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=4/4  value=4/4

**Who We Are** (topic: `inv:about:detail`)
- 6 items (of 16 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=6/6  value=6/6

**Leadership & Staff** (topic: `inv:leadership:staff`)
- 40 items (of 40 total in topic), dominant kind: `staff`
- diagnosed: `person_card` (high)
- fields with data: name=40/40  role=40/40  bio=5/40  email=32/40  headshot=37/40  ministry_area=27/40

**Kids Ministry** (topic: `inv:kids:program`)
- 8 items (of 9 total in topic), dominant kind: `program`
- diagnosed: `ministry_program_card` (high)
- fields with data: name=8/8  description=8/8  audience=8/8

**Students / Youth** (topic: `inv:students:program`)
- 8 items (of 8 total in topic), dominant kind: `program`
- diagnosed: `ministry_program_card` (medium)
- fields with data: name=8/8  description=8/8  audience=8/8  location=1/8

**College / Young Adults** (topic: `inv:college:program`)
- 6 items (of 9 total in topic), dominant kind: `program`
- diagnosed: `ministry_program_card` (medium)
- fields with data: name=6/6  description=6/6  audience=6/6

**College / Young Adults** (topic: `inv:college:detail`)
- 3 items (of 9 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=3/3  value=3/3

**Missions & Outreach** (topic: `inv:missions:program`)
- 33 items (of 41 total in topic), dominant kind: `program`
- diagnosed: `blog_post_card` (low)
- fields with data: title=33/33  date=1/33  body=33/33

**Missions & Outreach** (topic: `inv:missions:detail`)
- 4 items (of 41 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=4/4  value=4/4

**Missions & Outreach** (topic: `inv:missions:faq`)
- 2 items (of 41 total in topic), dominant kind: `faq`
- diagnosed: (unclassified)
- fields with data: answer=2/2  question=2/2

**New Here / First-Time** (topic: `inv:new_here:faq`)
- 6 items (of 17 total in topic), dominant kind: `faq`
- diagnosed: `faq_qna` (low)
- fields with data: question=6/6  answer=6/6

**New Here / First-Time** (topic: `inv:new_here:detail`)
- 4 items (of 17 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=4/4  value=4/4

**Baptism** (topic: `inv:baptism:detail`)
- 2 items (of 4 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=2/2  value=2/2

**Events / Calendar** (topic: `inv:events:program`)
- 33 items (of 33 total in topic), dominant kind: `program`
- diagnosed: `event_card` (high)
- fields with data: name=33/33  description=33/33  audience=30/33  start_date=3/33  cost=1/33

**Worship & Music** (topic: `inv:worship_music:program`)
- 18 items (of 22 total in topic), dominant kind: `program`
- diagnosed: `group_card` (low)
- fields with data: name=18/18  description=18/18  audience=14/18  duration=1/18

**Counseling** (topic: `inv:counseling:detail`)
- 4 items (of 8 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=4/4  value=4/4

**Counseling** (topic: `inv:counseling:staff`)
- 2 items (of 8 total in topic), dominant kind: `staff`
- diagnosed: `person_card` (medium)
- fields with data: name=2/2  role=2/2  bio=2/2  headshot=2/2

**Sermons / Messages** (topic: `inv:sermons:sermon`)
- 20 items (of 28 total in topic), dominant kind: `sermon`
- diagnosed: `sermon_card` (high)
- fields with data: title=20/20  series=10/20  speaker=15/20  date=20/20  video_url=20/20  audio_url=9/20  transcript_url=12/20  bulletin_url=15/20

**Sermons / Messages** (topic: `inv:sermons:detail`)
- 2 items (of 28 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=2/2  value=2/2

**Location & Contact** (topic: `inv:location_contact:contact_block`)
- 60 items (of 125 total in topic), dominant kind: `contact_block`
- diagnosed: (unclassified)
- fields with data: email=60/60  label=60/60  phone=55/60

**Location & Contact** (topic: `inv:location_contact:location_info`)
- 54 items (of 125 total in topic), dominant kind: `location_info`
- diagnosed: (unclassified)
- fields with data: label=54/54  address=54/54

**Location & Contact** (topic: `inv:location_contact:detail`)
- 3 items (of 125 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=3/3  value=3/3

**Other / Unclassified** (topic: `inv:other:link`)
- 106 items (of 201 total in topic), dominant kind: `link`
- diagnosed: `Resources` (low)
- fields with data: name=106/106  target_url=106/106

**Other / Unclassified** (topic: `inv:other:program`)
- 17 items (of 201 total in topic), dominant kind: `program`
- diagnosed: `blog_post_card` (low)
- fields with data: title=17/17  author=1/17  date=1/17  body=17/17  category=3/17

**Other / Unclassified** (topic: `inv:other:detail`)
- 15 items (of 201 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=15/15  value=15/15

**Care** (topic: `inv:care:program`)
- 11 items (of 14 total in topic), dominant kind: `program`
- diagnosed: `Resources` (low)
- fields with data: name=11/11  description=11/11  target_url=2/11

**Care** (topic: `inv:care:staff`)
- 2 items (of 14 total in topic), dominant kind: `staff`
- diagnosed: `person_card` (medium)
- fields with data: name=2/2  role=2/2  headshot=2/2

**Serve / Volunteer** (topic: `inv:serve:program`)
- 4 items (of 4 total in topic), dominant kind: `program`
- diagnosed: `feature_card` (low)
- fields with data: name=4/4  description=4/4  cta_label=4/4  cta_url=4/4

**Sundays / Services** (topic: `inv:sundays:program`)
- 2 items (of 2 total in topic), dominant kind: `program`
- diagnosed: `service_time` (high)
- fields with data: name=2/2  when=1/2  description=2/2

**Blog / News** (topic: `inv:blog_news:blog_news`)
- 26 items (of 34 total in topic), dominant kind: `blog_news`
- diagnosed: `blog_post_card` (high)
- fields with data: title=26/26  author=11/26  date=10/26  excerpt=14/26  url=26/26

**Blog / News** (topic: `inv:blog_news:detail`)
- 5 items (of 34 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=5/5  value=5/5

**Giving** (topic: `inv:giving:program`)
- 5 items (of 6 total in topic), dominant kind: `program`
- diagnosed: `feature_card` (low)
- fields with data: name=5/5  description=5/5  cta_label=3/5  cta_url=3/5

**Newsletter & Bulletin** (topic: `inv:newsletter_bulletin:newsletter_issue`)
- 5 items (of 8 total in topic), dominant kind: `newsletter_issue`
- diagnosed: `Resources` (high)
- fields with data: name=5/5  target_url=5/5  date=5/5

**Newsletter & Bulletin** (topic: `inv:newsletter_bulletin:detail`)
- 2 items (of 8 total in topic), dominant kind: `detail`
- diagnosed: (unclassified)
- fields with data: label=2/2  value=2/2
